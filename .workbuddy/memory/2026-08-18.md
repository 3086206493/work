# 2026-08-18

## 实战作业（一）：数据库查询工具 + 数据导出

完成汤豪的实战作业一：在"数据库查询工具"基础上新增数据导出功能。
参考项目为程文旭作业（HR 主题），刻意做出差异化：

- 主题：图书馆借阅系统（books/members/borrows），非 HR
- 导出格式：CSV + JSON + **HTML 可视化报告**（表格+SVG柱状图+统计），效果不同
- 自动化：多格式一次导出流水线 `pipeline.py` + Makefile/scripts/命令模板，非单一格式 auto_export
- 交互：自然语言意图识别（"导出csv/生成报表/全部导出"）+ 快捷键 e，非 y/csv/json/n
- 后端：仅 SQLite 零依赖（刻意区别于参考的 MySQL 双后端）

验证：`python verify.py` 5/5 通过。提交物：项目代码 + FEATURE_EXPORT.md（必需）+ README.md。
运行入口：`python run.py`（REPL）；导出：`python -m db_query_tool.pipeline --sql "..." --formats all`。
补充交付物：RUNBOOK.md（运行手册，含环境准备/三种运行方式/FAQ）。
